# Dev image: install deps only; source is mounted at runtime for hot reload.
FROM oven/bun:1
WORKDIR /app

COPY app/package.json app/bun.lock ./
RUN bun install --frozen-lockfile

EXPOSE 5800
ENV PORT=5800
CMD ["bun", "run", "dev"]
